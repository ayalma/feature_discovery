enum DescribedFeatureContentOrientation {
  above,
  below,
}

enum OverlayState {
  closed,
  opening,
  pulsing,
  activating,
  dismissing,
}

enum ContentOrientation {
  above,
  below,
  trivial,
}